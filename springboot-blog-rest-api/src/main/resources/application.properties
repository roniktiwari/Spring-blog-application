


# SPRING DATA JPA DEPENDENCY

spring.datasource.url = jdbc:mysql://localhost:3306/myblog?useSSL=false&serverTimeZone=UTC
spring.datasource.username= root
spring.datasource.password = root

# Hibernate properties


# we specify dialect so that hibernate can generate
# appropriate type of SQL statement for corresponding database configured

spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

spring.jpa.hibernate.ddl-auto=update
